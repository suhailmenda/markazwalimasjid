rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read prayer times, only authenticated admins can update them
    match /settings/prayerTimes {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
